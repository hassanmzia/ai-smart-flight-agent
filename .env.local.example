# =============================================================================
# Local / Virtual Environment Configuration
# =============================================================================
# Copy this file to .env.local and fill in your values:
#   cp .env.local.example .env.local
#
# This file is used when running WITHOUT Docker (Python venv + npm).
# For Docker, use .env.example instead.
# =============================================================================

# --- Database (local PostgreSQL) ---
# Option 1: Full local install (brew install postgresql / apt install postgresql)
DATABASE_URL=postgresql://travel_admin:travel_secure_pass_2026@localhost:5432/travel_agent_db

# Option 2: Use SQLite for quick local dev (no PostgreSQL needed)
# DATABASE_URL=sqlite:///db.sqlite3

# --- Django ---
SECRET_KEY=django-insecure-local-dev-key-change-in-production
DEBUG=True
ALLOWED_HOSTS=localhost,127.0.0.1,0.0.0.0

# --- Redis (local install or Docker) ---
# Option 1: Local Redis (default port 6379)
REDIS_URL=redis://localhost:6379/0

# Option 2: Skip Redis (use in-memory cache - limited functionality)
# REDIS_URL=

# --- RabbitMQ (local install or Docker) ---
# Option 1: Local RabbitMQ (default port 5672)
CELERY_BROKER_URL=amqp://guest:guest@localhost:5672/

# Option 2: Use Redis as broker instead of RabbitMQ
# CELERY_BROKER_URL=redis://localhost:6379/1

# --- CORS ---
CORS_ALLOWED_ORIGINS=http://localhost:3090,http://127.0.0.1:3090,http://localhost:5173

# --- API Keys ---
OPENAI_API_KEY=your_openai_api_key_here
SERP_API_KEY=your_serpapi_key_here
STRIPE_SECRET_KEY=your_stripe_secret_key_here
STRIPE_PUBLISHABLE_KEY=your_stripe_publishable_key_here
WEATHER_API_KEY=your_weather_api_key_here

# --- Frontend URLs (point to local backend) ---
VITE_API_URL=http://localhost:8109
VITE_WS_URL=ws://localhost:8109
VITE_MCP_URL=http://localhost:8107
PORT=3090

# --- MCP Server ---
MCP_PORT=8107
MCP_SERVER_URL=http://localhost:8107

# --- Email (console backend for local dev) ---
EMAIL_BACKEND=django.core.mail.backends.console.EmailBackend

# --- JWT ---
JWT_SECRET_KEY=local-jwt-secret-key
JWT_ALGORITHM=HS256
JWT_EXPIRATION_DELTA=3600

# --- Infrastructure Services (for docker-only mode) ---
# Set USE_DOCKER_SERVICES=true to start only Postgres/Redis/RabbitMQ via Docker
# while running app services in venv
USE_DOCKER_SERVICES=true
